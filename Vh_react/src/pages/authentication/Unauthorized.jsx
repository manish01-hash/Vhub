import React from "react";
import API_BASE_URL from "../../config";

const Unauthorized = () => {
  return <h1> Unauthorized User......</h1>;
};

export default Unauthorized; // Ensure this line is present
